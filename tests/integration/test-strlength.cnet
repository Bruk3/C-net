
void foo(string[] a, int i){
    stdout.writeln(a[i]);
    stdout.writeln(soi(a[i].slength()));
    return;
}

int main (){
    string[] arr = new string[3] {"cnet","is", "cool"};
    int i =0;
    while (i < arr.alength()){
        foo(arr,i);
        i = i+1;
    }
    return 0;
}