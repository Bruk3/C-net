int gcd(int a,int b){
    	while(a!=b) {
     	if(a>b)
          	a -= b;
          else
               b -= a;
    	}
    	return a;
}
int main (){
	int b = gcd(12,6); 
    stdout.writeln(soi(b));
    return 0;
}
