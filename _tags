#
#
# NOTE: Uncommenting this line will make the github actions workflow fail
# since the environment doesn't have llvm installed.
# Include the llvm and llvm.analysis packages while compiling
# true: package(llvm), package(llvm.analysis)

# Enable almost all compiler warnings
# TODO: Fix warning 42
true : warn(+a-4-42)
